import 'package:dart_application_1/morse_tree.dart';
import 'package:dart_application_1/input_parser.dart';

void main(List<String> arguments) {
    var text = ".. / .-.. --- ...- . / -.-- --- ..- / -... .- -... -.--";
    parseMorseText(text);
}
